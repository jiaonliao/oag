## 语言设置

**使用中文回复用户**

## 代码风格规范

### 注释规范
- **所有代码注释必须使用英文**
- 注释尽量详细，覆盖关键逻辑与边界


### 代码质量
- 遵循项目现有的代码风格和约定
- 保持代码简洁，避免过度工程化

## Skills 使用规范

### CRKnife Skills 触发场景
- crknife-guide : 当涉及到框架问题、工具查找、模块选型、配置问题时使用 crknife-guide
- crknife-api-layer ：当涉及到api模块代码修改时使用 crknife-api-layer
- crknife-service-layer-spec ：当涉及到service模块代码修改时使用 crknife-service-layer-spec
- crknife-repository-layer-spec ：当涉及到repository模块代码修改时使用 crknife-service-layer-spec
- crknife-i18n-keys-spec ：但需要新增或修改i18 key时使用crknife-i18n-keys-spec
- crknife-feign-remote-api：当涉及到 Feign 远程调用、xxx-api 模块引用、IRemote*Api 接口、服务间调用时使用 crknife-feign-remote-api

### Skills 主动触发规则

识别用户意图后主动调用对应 skill，不要等待用户明确要求：
- 涉及多层代码时按需调用多个 skill（如新增完整功能需要 API + Service + Repository）
- 代码审查时根据代码层级调用对应 skill

### Skills 调用汇总

任务完成时，输出本次任务中调用的所有 skills 列表：
- 格式：`📋 本次调用的 Skills: skill-name-1, skill-name-2, ...`
- 如果未调用任何 skill，输出：`📋 本次调用的 Skills: 无`

## 通用行为准则
- 遵循"足够简单"原则：只做必要的事情
- 保持专业和客观的态度

## 代码修改完成后执行
- mvn -DskipTests spotless:apply
- mvn -DskipTests spotless:check
